# ТЗ: Универсальная панель действий «Action Bar» в Дворце

## 1. Цель

Единая панель действий под **каждым ответом внутри раздела Дворец**: анализ ИИ, поиск в интернете с контекстом, сохранение (всё/цитата), пролистывание длинных ответов, возврат к списку. Замена разрозненных ad-hoc кнопок единым механизмом.

## 2. Аудит текущего состояния

| Область | Статус |
|---|---|
| Главное меню Дворца (6 кнопок) | ✅ уже инлайн (`cmd_palace`) |
| Все вложенные меню/экраны | ✅ уже инлайн (callback_data) |
| Списки записей → чтение записи | ✅ инлайн-кнопки `p_rd:{idx}` |
| AI-анализ комнаты | 🟡 есть (`_answer_room_ai`), но кнопки ad-hoc |
| Web-уточнение | 🟡 есть (`p_room_ai_web`), привязано только к AI-ответу комнаты |
| Сохранение всё/цитата | 🟡 есть мастер (`save.py`), но входит только через отдельные кнопки |
| Пагинация | 🟡 `p_cr:` на 3500 (устаревший) + автоплит 4000 (новый, конфликтует) |

## 3. Утверждённые решения

1. **Единая схема пагинации: 1500 символов + кнопки ◀️/▶️.** Убрать автоплит 4000 (несколько сообщений) — все ответы Дворца: одно сообщение, прокрутка кнопками.
2. **Анализ ИИ — подменю из 2 режимов:** «Анализ ответа» (только текст ответа) / «Анализ с контекстом комнаты» (ответ + записи комнаты, как `_answer_room_ai`).
3. **Web-поиск: авто-запрос от ИИ + возможность исправить** (ИИ предлагает запрос → подтвердить или ввести свой → поиск с контекстом ответа).
4. **Сохранение — подменю из 2 вариантов:** «В Дворец» (мастер `save.py`: крыло→комната→подтверждение) / «В my_notes» (файл `.md` в `NOTES_DIR`).
5. **Область: только Дворец.** Обычный диалог не трогаем (там уже реакции 👍❤️🤷 и `web_search`).

## 4. Требования (функциональные)

**F1. Панель под каждым ответом Дворца:**

```
[🤖 Анализ ИИ] [🌐 Поиск в интернете] [💾 Сохранить]
[◀️ Назад] [📄 2/5] [▶️ Вперёд]      ← только если длина > 1500
[🔙 Вернуться к списку]
```

**F2. Пагинация:** `paginate(text, limit=1500)` — страницы по 1500 символов; индикатор «стр N/M»; ◀️/▶️ переключают страницы (edit того же сообщения); кнопка «🔙» — отдельная, контекстная.

**F3. Анализ ИИ:** подменю: «📝 Анализ ответа» / «🏰 С контекстом комнаты» → результат — новое сообщение с той же панелью (рекурсия). Контекст комнаты берётся из `_user_context[uid]`.

**F4. Web-поиск:** ИИ генерирует запрос из текста ответа (1 AI-вызов) → сообщение «🔎 Запрос: «...» [✅ Искать] [✏️ Исправить]» → поиск `services.web_search.search_web` + системный промпт с контекстом ответа → результат с панелью.

**F5. Сохранение:** подменю «🏰 В Дворец» / «📝 В my_notes» / «❌ Отмена». Дворец → мастер `save.py` с предзаполненным `_save_state`. my_notes → файл `notes_YYYYMMDD_HHMM.md`, ответ о пути.

**F6. Возврат к списку:** `parent_cb` в контексте ответа (например `p_rdb` → список записей комнаты, `p_nav` → меню навигации). На верхнем уровне (главное меню) — кнопка не показывается.

**F7. Списки записей:** остаются инлайн-кнопками чтения (уже так), форматирование единообразно через общий рендер.

## 5. Архитектура

**Новый модуль `handlers/palace/action_bar.py`:**

```
action_bar.py
├── @dataclass Answer: text, title, ctx{wing, room, drawer, parent_cb}
├── answer_store: TtlDict[uid] → Answer        # TTL 30 мин
├── paginate(text, limit=1500) -> list[str]    # чистая функция
├── build_action_bar(uid, answer, page_idx) -> kb
├── finalize_answer(uid, edit_func, text, ctx) # точка входа: пагинация + отрисовка + store
├── ab_ai      → подменю (режимы) → _run_ai_analysis
├── ab_web     → авто-запрос → подтверждение → _run_web_search
├── ab_sv      → подменю (Дворец/my_notes)
├── ab_pg:<i>  → переключение страницы (edit)
└── ab_back    → вызов parent_cb
```

**Переиспользование (без дублирования):** `get_current_ai`/`_sync_ai_call` (ai_engine), `search_web` (web_search), мастер `save.py` (`_save_state`, `_show_save_wings`), `safe_edit_text` (_utils), `TtlDict`.

**Почему так:** DRY (сейчас кнопки дублируются в 10+ местах), единый UX, тестируемость (чистые функции без сети), расширяемость (новая кнопка = 1 строка), минимальный diff (логика хендлеров не меняется — только вызов `finalize_answer` вместо kb-сборки).

## 6. Точки интеграции

| Файл | Что меняется |
|---|---|
| `navigation.py` | `_show_drawers_page`, `cb_read_drawer` (замена автоплита на пагинацию 1500), `_answer_room_ai`, `cb_cross_ai_article`, удаление старого `p_cr:` |
| `__init__.py` | `process_mcp_input` (списки комнат, траверс, туннели) |
| `admin.py` | `palace_status`, инструкции, результаты обслуживания |
| `kg.py` | `_send_kg_page`, результаты поиска сущностей |
| `tunnels.py` | результаты |
| `save.py` | без изменений (только вызов мастера), + ветка «В my_notes» |

## 7. Ограничения и риски

- callback data ≤ 64 байта → пагинация через sid-карту (`_room_callback_map`), текст в data не класть
- Python 3.14: `InlineKeyboardBuilder` импортировать локально внутри функций
- Убрать автоплит 4000 в `cb_read_drawer` (возврат к edit-схеме) — обратная совместимость: старые сообщения `p_cr:` оставить с деградацией (alert «откройте заново»)
- 1500-символьные страницы без разрыва слов? — решение: резать по границе абзаца/слова (смягчение), не строго по байтам

## 8. Тестирование (AGENTS.md)

- **pytest** (`tests/test_action_bar.py`): `paginate` BVA (1499/1500/1501, 2999/3000/3001, пустой текст, разрыв по словам), store (TTL, перезапись, чужой uid), сборка kb (состав кнопок при разной длине/контексте), колбэки с mocked bot
- **.feature** `features/palace_action_bar.feature`: 1 happy path + 3 негатива (нет контекста, ошибка ИИ, пустой ответ) + 2 edge (ровно 1500, >3000), теги @smoke @regression
- `make qa` после каждого шага; покрытие ≥80% нового модуля; мутации ≤10%

## 9. Порядок работ

1. `make qa` + бейзлайн покрытия/мутантов (§8.1 AGENTS.md) + коммит «pre-refactor baseline»
2. `action_bar.py` + `paginate` + тесты (этап: чистые функции)
3. Store + kb + колбэки ab_ai/ab_web/ab_sv/ab_back + тесты
4. Интеграция `navigation.py` → `make qa`
5. Интеграция `__init__.py`, `admin.py`, `kg.py`, `tunnels.py` (по одному, `make qa` между)
6. `.feature` + полный цикл `make qa`, `make cov`, `make mutate`

## 10. Критерии приёмки

- ✅ Панель под каждым ответом Дворца (статус, навигация, записи, KG, туннели, AI, статья)
- ✅ Один ответ >1500 → одно сообщение + ◀️/▶️ + «N/M»; длинная запись читается целиком прокруткой
- ✅ «💾 Сохранить»: всё/цитата → Дворец или my_notes
- ✅ «🌐 Поиск»: предложенный ИИ запрос → подтверждение → результат с панелью
- ✅ «🤖 Анализ»: 2 режима, результат с панелью
- ✅ «🔙» возвращает к родительскому списку
- ✅ Обычный диалог не изменён; все старые тесты зелёные; `make qa` ✅
